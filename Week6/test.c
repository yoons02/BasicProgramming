#include <stdio.h>

int main()
{
    float a,b,r,x,y,dis;


    printf("Enter the radius of the circle \'r\' : ");
    scanf(" %f",&r);
    printf("Enter the coordinates of points to check (x,y) : ");
    scanf(" %f %f\n",&x,&y);
}